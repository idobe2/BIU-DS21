import numpy as np

# Create two NumPy arrays and compute their element-wise multiplication
arr_A = np.array([1, 2, 3, 4, 5])
arr_B = np.array([10, 20, 30, 40, 50])
arr_C = np.multiply(arr_A, arr_B)
print("Array:", arr_C)